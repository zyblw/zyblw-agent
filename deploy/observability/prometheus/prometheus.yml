global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/zyblw-agent-alerts.yml

scrape_configs:
  - job_name: zyblw-agent-otel-collector
    static_configs:
      - targets: ["otel-collector:9464"]
